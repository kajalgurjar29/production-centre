# Admin Panel — build output goes here

Drop the production build here (the folder that contains `index.html` and
the built `assets/`/`static/` files) — copy its **contents** directly into
this folder, so `index.html` ends up at:

```
frontend/admin-panel/index.html
```

Served at: `/admin`
